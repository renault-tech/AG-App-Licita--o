# P2 UX — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar quatro melhorias de UX em sequência: auto-save universal nos editores, status real das etapas no nav do processo, busca com paginação na lista de processos, e empty states nas telas principais.

**Architecture:** D (hook + componente + 6 editores) → B (server action + layout) → C (2 componentes + atualizar page.tsx) → A (componente + 4 telas). Nenhuma nova dependência externa, nenhuma migration Supabase.

**Tech Stack:** Next.js 14 App Router, TypeScript estrito, React hooks, shadcn/ui, Tailwind CSS, Lucide React, Supabase `@supabase/ssr`.

---

## Mapa de Arquivos

**Criar:**
- `src/hooks/use-auto-save.ts`
- `src/components/licita/auto-save-indicator.tsx`
- `src/app/(dashboard)/processos/busca-processos.tsx`
- `src/app/(dashboard)/processos/paginacao-processos.tsx`
- `src/components/licita/empty-state.tsx`

**Modificar:**
- `src/app/(dashboard)/processos/[id]/dfd/editor-dfd.tsx`
- `src/app/(dashboard)/processos/[id]/riscos/editor-riscos.tsx`
- `src/app/(dashboard)/processos/[id]/etp/editor-etp.tsx`
- `src/app/(dashboard)/processos/[id]/tr/editor-tr.tsx`
- `src/app/(dashboard)/processos/[id]/edital/editor-edital.tsx`
- `src/app/(dashboard)/processos/[id]/parecer/editor-parecer.tsx`
- `src/lib/actions/processo.ts`
- `src/app/(dashboard)/processos/[id]/layout.tsx`
- `src/app/(dashboard)/processos/page.tsx`
- `src/app/(dashboard)/processos/[id]/cotacao/cotacao-form.tsx`
- `src/app/(dashboard)/procuradoria/lista-pareceres.tsx`
- `src/app/(dashboard)/admin/organizacoes/page.tsx`

---

## Task 1: Hook `useAutoSave`

**Files:**
- Create: `src/hooks/use-auto-save.ts`

- [ ] **Step 1: Criar o arquivo**

```ts
// src/hooks/use-auto-save.ts
'use client'

import { useState, useEffect, useRef } from 'react'

export type AutoSaveStatus = 'idle' | 'dirty' | 'saving' | 'saved' | 'error'

/**
 * Hook de auto-save com debounce.
 * deps: lista de valores a observar — qualquer mudança dispara o debounce.
 * onSave: função assíncrona sem argumento (usa closure para ler estado atual).
 * delay: ms de debounce (default 1500).
 */
export function useAutoSave(
  deps: unknown[],
  onSave: () => Promise<void>,
  delay = 1500
): { status: AutoSaveStatus; lastSavedAt: Date | null; retrySave: () => void } {
  const [status, setStatus]         = useState<AutoSaveStatus>('idle')
  const [lastSavedAt, setLastSaved] = useState<Date | null>(null)
  const timerRef   = useRef<ReturnType<typeof setTimeout> | null>(null)
  const onSaveRef  = useRef(onSave)
  const mountedRef = useRef(false)

  // Mantém referência sempre atualizada sem re-disparar o effect
  useEffect(() => { onSaveRef.current = onSave }, [onSave])

  // Aviso ao sair com alterações não salvas
  useEffect(() => {
    const handler = (e: BeforeUnloadEvent) => {
      if (status === 'dirty' || status === 'saving') {
        e.preventDefault()
        e.returnValue = ''
      }
    }
    window.addEventListener('beforeunload', handler)
    return () => window.removeEventListener('beforeunload', handler)
  }, [status])

  // Debounce quando deps mudam (ignora montagem inicial)
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => {
    if (!mountedRef.current) {
      mountedRef.current = true
      return
    }
    setStatus('dirty')
    if (timerRef.current) clearTimeout(timerRef.current)
    timerRef.current = setTimeout(async () => {
      setStatus('saving')
      try {
        await onSaveRef.current()
        setLastSaved(new Date())
        setStatus('saved')
      } catch {
        setStatus('error')
      }
    }, delay)
    return () => { if (timerRef.current) clearTimeout(timerRef.current) }
  }, deps) // eslint-disable-line react-hooks/exhaustive-deps

  function retrySave() {
    setStatus('saving')
    onSaveRef.current()
      .then(() => { setLastSaved(new Date()); setStatus('saved') })
      .catch(() => setStatus('error'))
  }

  return { status, lastSavedAt, retrySave }
}
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/use-auto-save.ts
git commit -m "feat(ux): hook useAutoSave com debounce, beforeunload e retry"
```

---

## Task 2: Componente `AutoSaveIndicator`

**Files:**
- Create: `src/components/licita/auto-save-indicator.tsx`

- [ ] **Step 1: Criar o arquivo**

```tsx
// src/components/licita/auto-save-indicator.tsx
'use client'

import { Loader2, Check, AlertCircle } from 'lucide-react'
import type { AutoSaveStatus } from '@/hooks/use-auto-save'

interface AutoSaveIndicatorProps {
  status: AutoSaveStatus
  lastSavedAt: Date | null
  onRetry: () => void
  className?: string
}

function formatarHora(d: Date) {
  return d.toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' })
}

export function AutoSaveIndicator({ status, lastSavedAt, onRetry, className = '' }: AutoSaveIndicatorProps) {
  if (status === 'idle') return null

  return (
    <span className={`flex items-center gap-1.5 text-xs ${className}`} aria-live="polite">
      {status === 'dirty' && (
        <span style={{ color: 'var(--muted)' }}>Alteracoes pendentes...</span>
      )}
      {status === 'saving' && (
        <>
          <Loader2 className="w-3 h-3 animate-spin" style={{ color: 'var(--muted)' }} />
          <span style={{ color: 'var(--muted)' }}>Salvando...</span>
        </>
      )}
      {status === 'saved' && (
        <>
          <Check className="w-3 h-3" style={{ color: 'var(--success)' }} />
          <span style={{ color: 'var(--success)' }}>
            Salvo{lastSavedAt ? ` as ${formatarHora(lastSavedAt)}` : ''}
          </span>
        </>
      )}
      {status === 'error' && (
        <>
          <AlertCircle className="w-3 h-3" style={{ color: 'var(--destructive)' }} />
          <span style={{ color: 'var(--destructive)' }}>
            Erro ao salvar{' '}
            <button
              type="button"
              onClick={onRetry}
              className="underline"
              style={{ color: 'var(--destructive)' }}
            >
              tentar novamente
            </button>
          </span>
        </>
      )}
    </span>
  )
}
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/components/licita/auto-save-indicator.tsx
git commit -m "feat(ux): componente AutoSaveIndicator (idle/dirty/saving/saved/error)"
```

---

## Task 3: Auto-save no EditorDFD

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/dfd/editor-dfd.tsx`

O editor tem `handleSalvar` que salva `objeto`, `justificativa`, `fiscal`, `dotacao` + itens. O auto-save salva somente os campos de texto (não itens — estes têm ações explícitas de add/remove).

- [ ] **Step 1: Adicionar imports e hook ao EditorDFD**

No topo do arquivo, adicionar após os imports existentes:

```ts
import { useAutoSave } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente `EditorDFD`, logo após a declaração de `salvando`:

```ts
const autoSalvarCampos = useCallback(async () => {
  if (readonly) return
  await atualizarDFD(dfd.id, {
    objeto,
    justificativa_necessidade: justificativa,
    fiscal_contrato: fiscal,
    dotacao_orcamentaria: dotacao,
  })
}, [dfd.id, objeto, justificativa, fiscal, dotacao, readonly])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [objeto, justificativa, fiscal, dotacao],
  autoSalvarCampos,
)
```

- [ ] **Step 2: Adicionar AutoSaveIndicator no CardFooter**

No `CardFooter`, localizar o `div` com os botões à direita (que contém o botão Salvar). Adicionar o indicador ao lado do botão de salvar:

```tsx
<div className="flex items-center gap-2">
  {!readonly && (
    <AutoSaveIndicator
      status={autoSaveStatus}
      lastSavedAt={lastSavedAt}
      onRetry={retrySave}
    />
  )}
  <Button
    onClick={handleSalvar}
    disabled={salvando || readonly}
    className="bg-blue-700 hover:bg-blue-800 text-white gap-2 h-9 text-sm"
  >
    {salvando
      ? <><Loader2 className="w-4 h-4 animate-spin" /> Salvando...</>
      : <><Save className="w-4 h-4" /> Salvar</>}
  </Button>
  <Link href={`/processos/${processoId}/cotacao`}>
    <Button variant="outline" className="gap-1.5 h-9 text-sm">
      Proxima etapa
      <ChevronRight className="w-4 h-4" />
    </Button>
  </Link>
</div>
```

- [ ] **Step 3: Verificar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/app/\(dashboard\)/processos/\[id\]/dfd/editor-dfd.tsx
git commit -m "feat(ux): auto-save nos campos de texto do EditorDFD"
```

---

## Task 4: Auto-save no EditorRiscos

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/riscos/editor-riscos.tsx`

O editor tem `handleSalvar` que chama `atualizarMapaRiscos(mapa.id, riscos)`.

- [ ] **Step 1: Adicionar imports e hook**

No topo do arquivo, após os imports existentes:

```ts
import { useAutoSave } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente, após a declaração de `salvando`:

```ts
const autoSalvarRiscos = useCallback(async () => {
  if (!podeEditar || mapa.status === 'assinado') return
  await atualizarMapaRiscos(mapa.id, riscos)
}, [mapa.id, mapa.status, riscos, podeEditar])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [riscos],
  autoSalvarRiscos,
)
```

Adicionar `useCallback` ao import do React: `import { useState, useCallback } from 'react'`

- [ ] **Step 2: Adicionar AutoSaveIndicator no CardFooter**

Localizar o `CardFooter` do componente. Adicionar antes ou ao lado do botão Salvar:

```tsx
{podeEditar && mapa.status !== 'assinado' && (
  <AutoSaveIndicator
    status={autoSaveStatus}
    lastSavedAt={lastSavedAt}
    onRetry={retrySave}
    className="mr-auto"
  />
)}
```

- [ ] **Step 3: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/riscos/editor-riscos.tsx
git commit -m "feat(ux): auto-save no EditorRiscos"
```

---

## Task 5: Auto-save no EditorETP

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/etp/editor-etp.tsx`

O editor tem `formData: FormData` (8 campos de texto) e `handleSalvar` que chama `atualizarETP(etp.id, formData)`.

- [ ] **Step 1: Adicionar imports e hook**

No topo:

```ts
import { useCallback } from 'react'
import { useAutoSave } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente, após declaração de `salvando`:

```ts
const autoSalvarETP = useCallback(async () => {
  if (!podeEditar || etp.status === 'assinado') return
  await atualizarETP(etp.id, formData)
}, [etp.id, etp.status, formData, podeEditar])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [formData],
  autoSalvarETP,
)
```

- [ ] **Step 2: Adicionar AutoSaveIndicator no CardFooter**

Localizar o `CardFooter`. Adicionar o indicador ao lado do botão Salvar (mesma posição dos outros editores):

```tsx
{podeEditar && etp.status !== 'assinado' && (
  <AutoSaveIndicator
    status={autoSaveStatus}
    lastSavedAt={lastSavedAt}
    onRetry={retrySave}
    className="mr-auto"
  />
)}
```

- [ ] **Step 3: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/etp/editor-etp.tsx
git commit -m "feat(ux): auto-save no EditorETP"
```

---

## Task 6: Auto-save no EditorTR

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/tr/editor-tr.tsx`

Mesmo padrão do ETP: `formData` (10 campos) + `atualizarTR`.

- [ ] **Step 1: Adicionar imports e hook**

No topo:

```ts
import { useCallback } from 'react'
import { useAutoSave } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente, após `salvando`:

```ts
const autoSalvarTR = useCallback(async () => {
  if (!podeEditar || tr.status === 'assinado') return
  await atualizarTR(tr.id, formData)
}, [tr.id, tr.status, formData, podeEditar])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [formData],
  autoSalvarTR,
)
```

- [ ] **Step 2: Adicionar AutoSaveIndicator no CardFooter**

```tsx
{podeEditar && tr.status !== 'assinado' && (
  <AutoSaveIndicator
    status={autoSaveStatus}
    lastSavedAt={lastSavedAt}
    onRetry={retrySave}
    className="mr-auto"
  />
)}
```

- [ ] **Step 3: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/tr/editor-tr.tsx
git commit -m "feat(ux): auto-save no EditorTR"
```

---

## Task 7: Auto-save no EditorEdital

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/edital/editor-edital.tsx`

O editor tem `secoes: Secao[]` (array de objetos `{id, titulo, texto}`) e `handleSalvar` que chama `atualizarEdital(edital.id, secoes)`.

- [ ] **Step 1: Adicionar imports e hook**

No topo:

```ts
import { useCallback } from 'react'
import { useAutoSave } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente, após `salvando`:

```ts
const autoSalvarEdital = useCallback(async () => {
  if (!podeEditar || edital.status === 'assinado') return
  await atualizarEdital(edital.id, secoes)
}, [edital.id, edital.status, secoes, podeEditar])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [secoes],
  autoSalvarEdital,
)
```

- [ ] **Step 2: Adicionar AutoSaveIndicator no CardFooter**

```tsx
{podeEditar && edital.status !== 'assinado' && (
  <AutoSaveIndicator
    status={autoSaveStatus}
    lastSavedAt={lastSavedAt}
    onRetry={retrySave}
    className="mr-auto"
  />
)}
```

- [ ] **Step 3: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/edital/editor-edital.tsx
git commit -m "feat(ux): auto-save no EditorEdital"
```

---

## Task 8: Normalizar EditorParecer

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/parecer/editor-parecer.tsx`

O parecer tem lógica própria de auto-save inline (`useDebounce` local + `statusSalvo`). Substituir pelo hook `useAutoSave`.

- [ ] **Step 1: Substituir lógica de auto-save no EditorParecer**

Remover do arquivo:
- A função `useDebounce` (linhas 36–42)
- O estado `statusSalvo` (`useState<'salvo' | 'salvando' | 'idle'>`)
- O `autoSalvar` que usa `useDebounce`
- Os `setStatusSalvo` nas funções

Adicionar no topo:

```ts
import { useAutoSave, AutoSaveStatus } from '@/hooks/use-auto-save'
import { AutoSaveIndicator } from '@/components/licita/auto-save-indicator'
```

Dentro do componente, substituir `statusSalvo`/`autoSalvar` por:

```ts
const autoSalvarConteudo = useCallback(async () => {
  await salvarConteudo(parecer.id, conteudo)
}, [parecer.id, conteudo])

const { status: autoSaveStatus, lastSavedAt, retrySave } = useAutoSave(
  [conteudo],
  autoSalvarConteudo,
  2000,
)
```

Adicionar `useCallback` ao import do React.

- [ ] **Step 2: Substituir indicador de status no JSX**

Localizar onde o editor exibia `statusSalvo` (geralmente como texto `salvando...`/`salvo`). Substituir por:

```tsx
<AutoSaveIndicator
  status={autoSaveStatus}
  lastSavedAt={lastSavedAt}
  onRetry={retrySave}
/>
```

- [ ] **Step 3: Remover referências a `statusSalvo` e `useDebounce`**

Buscar no arquivo por `statusSalvo` e `useDebounce` — não deve sobrar nenhuma referência.

- [ ] **Step 4: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/parecer/editor-parecer.tsx
git commit -m "feat(ux): normalizar EditorParecer para usar hook useAutoSave"
```

---

## Task 9: Server Action `obterStatusEtapas`

**Files:**
- Modify: `src/lib/actions/processo.ts`

- [ ] **Step 1: Adicionar tipos e função ao final do arquivo**

Abrir `src/lib/actions/processo.ts` e acrescentar ao final:

```ts
// Tipos de status das etapas para o nav do processo
export type StatusEtapa = 'nao_iniciado' | 'rascunho' | 'em_revisao' | 'assinado' | 'devolvido'
export type EtapaStatus = { slug: string; status: StatusEtapa; updated_at: string | null }

// Mapeamento slug → tabela Supabase
const SLUG_TABELA: Array<{ slug: string; tabela: string; temStatus: boolean }> = [
  { slug: 'dfd',         tabela: 'dfd',               temStatus: true  },
  { slug: 'cotacao',     tabela: 'cotacoes',           temStatus: false },
  { slug: 'etp',         tabela: 'etp',                temStatus: true  },
  { slug: 'tr',          tabela: 'termo_referencia',   temStatus: true  },
  { slug: 'riscos',      tabela: 'mapa_riscos',        temStatus: true  },
  { slug: 'edital',      tabela: 'edital',             temStatus: true  },
  { slug: 'declaracao',  tabela: 'declaracoes',        temStatus: true  },
  { slug: 'oficio',      tabela: 'oficios',            temStatus: true  },
  { slug: 'parecer',     tabela: 'pareceres',          temStatus: true  },
  { slug: 'autorizacao', tabela: 'autorizacoes',       temStatus: true  },
  { slug: 'publicacao',  tabela: 'publicacoes',        temStatus: true  },
]

export async function obterStatusEtapas(processoId: string): Promise<EtapaStatus[]> {
  const supabase = await createClient()

  const resultados = await Promise.all(
    SLUG_TABELA.map(async ({ slug, tabela, temStatus }) => {
      const { data } = await (supabase as any)
        .from(tabela)
        .select(temStatus ? 'status, updated_at' : 'updated_at')
        .eq('processo_id', processoId)
        .maybeSingle()

      if (!data) {
        return { slug, status: 'nao_iniciado' as StatusEtapa, updated_at: null }
      }

      if (!temStatus) {
        // cotacoes: existe linha = rascunho
        return { slug, status: 'rascunho' as StatusEtapa, updated_at: data.updated_at ?? null }
      }

      const s = (data.status ?? 'rascunho') as StatusEtapa
      return { slug, status: s, updated_at: data.updated_at ?? null }
    })
  )

  // revisao: inferir do processo (fase_atual)
  const { data: processo } = await (supabase as any)
    .from('processos_licitatorios')
    .select('fase_atual')
    .eq('id', processoId)
    .maybeSingle()

  const faseAtual = processo?.fase_atual as string | null
  const revisaoStatus: StatusEtapa =
    faseAtual === 'setor_licitacao'  ? 'rascunho'
    : faseAtual === 'procurador'     ? 'assinado'
    : 'nao_iniciado'

  resultados.push({ slug: 'revisao', status: revisaoStatus, updated_at: null })

  return resultados
}
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/lib/actions/processo.ts
git commit -m "feat(ux): server action obterStatusEtapas para todas as tabelas de documento"
```

---

## Task 10: Status real das etapas no layout do processo

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/layout.tsx`

- [ ] **Step 1: Importar a nova função e tipos**

No topo do arquivo, adicionar ao import de `processo.ts`:

```ts
import { obterStatusEtapas } from '@/lib/actions/processo'
import type { EtapaStatus, StatusEtapa } from '@/lib/actions/processo'
```

- [ ] **Step 2: Chamar `obterStatusEtapas` em paralelo com as queries existentes**

Localizar o bloco de busca do processo (onde estão `supabase.from('processos_licitatorios')` e `supabase.from('tramitacao_historico')`). Adicionar a chamada em paralelo:

```ts
const [{ data: processo }, { data: historicoRaw }, etapasStatus] = await Promise.all([
  (supabase.from('processos_licitatorios') as any)
    .select('id, objeto, modalidade, status, numero_processo, valor_estimado, fase_atual, organizacao_id')
    .eq('id', id)
    .maybeSingle(),
  (supabase.from('tramitacao_historico') as any)
    .select('id, processo_id, organizacao_id, usuario_id, nome_usuario, de_papel, para_papel, tipo, motivo, pendencias, created_at')
    .eq('processo_id', id)
    .order('created_at', { ascending: true }),
  obterStatusEtapas(id),
])
```

- [ ] **Step 3: Substituir lógica de `isConcluida` no nav desktop**

Localizar o trecho do nav desktop (`hidden md:flex`) onde está `const isConcluida = i < etapaAtivaIndex`. Substituir a renderização de cada item por:

```tsx
{etapasVisiveis.map((etapa, i) => {
  const Icon = etapa.icon
  const isAtiva  = etapa.slug === etapaAtiva
  const dbStatus = (etapasStatus.find(e => e.slug === etapa.slug)?.status ?? 'nao_iniciado') as StatusEtapa
  const updated  = etapasStatus.find(e => e.slug === etapa.slug)?.updated_at

  const tooltipText = updated
    ? `Ultima edicao: ${new Date(updated).toLocaleString('pt-BR', { day:'2-digit', month:'2-digit', year:'numeric', hour:'2-digit', minute:'2-digit' })}`
    : dbStatus === 'nao_iniciado' ? 'Nao iniciado' : etapa.label

  const circleStyle: React.CSSProperties =
    isAtiva         ? { backgroundColor: 'var(--primary)',     borderColor: 'var(--primary)',    color: '#fff'             }
    : dbStatus === 'assinado'   ? { backgroundColor: 'var(--successWash)', borderColor: 'var(--success)',  color: 'var(--success)'   }
    : dbStatus === 'devolvido'  ? { backgroundColor: '#fef3c7',            borderColor: '#f59e0b',         color: '#b45309'          }
    : dbStatus === 'rascunho' || dbStatus === 'em_revisao'
                                ? { backgroundColor: 'var(--primaryWash)', borderColor: 'var(--primary)',  color: 'var(--primary)'   }
    :                             { backgroundColor: 'var(--surface)',     borderColor: 'var(--hairline)', color: 'var(--muted)'     }

  const labelColor =
    isAtiva         ? 'var(--primary)'
    : dbStatus === 'assinado'   ? 'var(--success)'
    : dbStatus === 'devolvido'  ? '#b45309'
    : dbStatus === 'rascunho' || dbStatus === 'em_revisao' ? 'var(--primary)'
    :                             'var(--muted)'

  const connectorColor =
    dbStatus === 'assinado' ? 'var(--success)'
    : dbStatus === 'rascunho' || dbStatus === 'em_revisao' ? 'var(--primary)'
    : 'var(--hairline)'

  return (
    <div key={etapa.slug} className="flex items-center flex-1 min-w-0">
      <Link
        href={`/processos/${id}/${etapa.slug}`}
        className="flex flex-col items-center gap-1 px-2 py-1 rounded-[var(--r-md)] transition-all flex-1 min-w-0"
        title={tooltipText}
      >
        <div
          className="w-7 h-7 rounded-full flex items-center justify-center border-2 transition-all"
          style={circleStyle}
        >
          {dbStatus === 'assinado' && !isAtiva ? (
            <CheckCircle2 className="w-4 h-4" />
          ) : (
            <Icon className="w-3.5 h-3.5" />
          )}
        </div>
        <span
          className={`text-xs font-medium whitespace-nowrap ${isAtiva ? 'font-semibold' : ''}`}
          style={{ color: labelColor }}
        >
          {etapa.label}
        </span>
      </Link>
      {i < etapasVisiveis.length - 1 && (
        <div
          className="h-0.5 flex-1 mx-1 rounded-full transition-all"
          style={{ backgroundColor: connectorColor }}
        />
      )}
    </div>
  )
})}
```

- [ ] **Step 4: Atualizar nav mobile (pills)**

Localizar o nav mobile (`md:hidden`). Substituir `isConcluida` por:

```tsx
{etapasVisiveis.map((etapa, i) => {
  const isAtiva  = etapa.slug === etapaAtiva
  const dbStatus = (etapasStatus.find(e => e.slug === etapa.slug)?.status ?? 'nao_iniciado') as StatusEtapa

  const pillStyle: React.CSSProperties =
    isAtiva         ? { backgroundColor: 'var(--primary)',     color: '#fff',              borderColor: 'var(--primary)'   }
    : dbStatus === 'assinado'   ? { backgroundColor: 'var(--successWash)', color: 'var(--success)',    borderColor: 'var(--success)'  }
    : dbStatus === 'rascunho' || dbStatus === 'em_revisao'
                                ? { backgroundColor: 'var(--primaryWash)', color: 'var(--primary)',    borderColor: 'var(--primary)'  }
    :                             { backgroundColor: 'var(--surface)',     color: 'var(--muted)',      borderColor: 'var(--hairline)' }

  return (
    <Link
      key={etapa.slug}
      href={`/processos/${id}/${etapa.slug}`}
      className="flex items-center gap-1.5 px-3 py-1.5 rounded-[var(--r-pill)] text-xs font-medium whitespace-nowrap border transition-all"
      style={pillStyle}
    >
      {dbStatus === 'assinado' && <CheckCircle2 className="w-3 h-3" />}
      {etapa.label}
    </Link>
  )
})}
```

- [ ] **Step 5: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/\[id\]/layout.tsx
git commit -m "feat(ux): status real das etapas no nav do processo (DB-driven)"
```

---

## Task 11: Componente `BuscaProcessos`

**Files:**
- Create: `src/app/(dashboard)/processos/busca-processos.tsx`

- [ ] **Step 1: Criar o arquivo**

```tsx
// src/app/(dashboard)/processos/busca-processos.tsx
'use client'

import { useState, useEffect, useRef } from 'react'
import { useRouter, useSearchParams } from 'next/navigation'
import { Search, X } from 'lucide-react'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'

export default function BuscaProcessos() {
  const router       = useRouter()
  const searchParams = useSearchParams()
  const [valor, setValor] = useState(searchParams.get('q') ?? '')
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)

  useEffect(() => {
    setValor(searchParams.get('q') ?? '')
  }, [searchParams])

  function handleChange(e: React.ChangeEvent<HTMLInputElement>) {
    const novo = e.target.value
    setValor(novo)
    if (timerRef.current) clearTimeout(timerRef.current)
    timerRef.current = setTimeout(() => {
      const params = new URLSearchParams(searchParams.toString())
      if (novo.trim()) {
        params.set('q', novo.trim())
      } else {
        params.delete('q')
      }
      params.set('page', '1')
      router.push(`/processos?${params.toString()}`)
    }, 300)
  }

  function handleLimpar() {
    setValor('')
    const params = new URLSearchParams(searchParams.toString())
    params.delete('q')
    params.set('page', '1')
    router.push(`/processos?${params.toString()}`)
  }

  return (
    <div className="relative flex-1 max-w-sm">
      <Search
        className="absolute left-2.5 top-1/2 -translate-y-1/2 w-3.5 h-3.5 pointer-events-none"
        style={{ color: 'var(--muted)' }}
        aria-hidden
      />
      <Input
        type="search"
        value={valor}
        onChange={handleChange}
        placeholder="Buscar por objeto ou numero..."
        className="pl-8 pr-8 h-9 text-sm"
        aria-label="Buscar processos"
      />
      {valor && (
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={handleLimpar}
          className="absolute right-1 top-1/2 -translate-y-1/2 h-6 w-6 p-0"
          aria-label="Limpar busca"
        >
          <X className="w-3 h-3" />
        </Button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/busca-processos.tsx
git commit -m "feat(ux): componente BuscaProcessos com debounce 300ms"
```

---

## Task 12: Componente `PaginacaoProcessos`

**Files:**
- Create: `src/app/(dashboard)/processos/paginacao-processos.tsx`

- [ ] **Step 1: Criar o arquivo**

```tsx
// src/app/(dashboard)/processos/paginacao-processos.tsx
import Link from 'next/link'
import { ChevronLeft, ChevronRight } from 'lucide-react'

interface PaginacaoProcessosProps {
  total:    number
  page:     number
  pageSize: number
  q?:       string
  status?:  string
  fase?:    string
}

function buildHref(page: number, q?: string, status?: string, fase?: string) {
  const params = new URLSearchParams()
  params.set('page', String(page))
  if (q)      params.set('q', q)
  if (status) params.set('status', status)
  if (fase)   params.set('fase', fase)
  return `/processos?${params.toString()}`
}

export default function PaginacaoProcessos({ total, page, pageSize, q, status, fase }: PaginacaoProcessosProps) {
  if (total <= pageSize) return null

  const totalPages = Math.ceil(total / pageSize)
  const inicio = (page - 1) * pageSize + 1
  const fim    = Math.min(page * pageSize, total)

  return (
    <div
      className="flex items-center justify-between pt-3 mt-2"
      style={{ borderTop: '1px solid var(--hairline)' }}
    >
      <span className="text-xs" style={{ color: 'var(--muted)' }}>
        Mostrando {inicio} a {fim} de {total} processos
      </span>
      <div className="flex items-center gap-1">
        {page > 1 ? (
          <Link
            href={buildHref(page - 1, q, status, fase)}
            className="flex items-center justify-center w-7 h-7 rounded border transition-colors"
            style={{ borderColor: 'var(--hairline)', color: 'var(--inkSoft)' }}
            aria-label="Pagina anterior"
          >
            <ChevronLeft className="w-3.5 h-3.5" />
          </Link>
        ) : (
          <span
            className="flex items-center justify-center w-7 h-7 rounded border opacity-40 cursor-not-allowed"
            style={{ borderColor: 'var(--hairline)', color: 'var(--muted)' }}
            aria-disabled="true"
          >
            <ChevronLeft className="w-3.5 h-3.5" />
          </span>
        )}

        <span className="text-xs font-semibold px-2" style={{ color: 'var(--ink)' }}>
          {page} / {totalPages}
        </span>

        {page < totalPages ? (
          <Link
            href={buildHref(page + 1, q, status, fase)}
            className="flex items-center justify-center w-7 h-7 rounded border transition-colors"
            style={{ borderColor: 'var(--hairline)', color: 'var(--inkSoft)' }}
            aria-label="Proxima pagina"
          >
            <ChevronRight className="w-3.5 h-3.5" />
          </Link>
        ) : (
          <span
            className="flex items-center justify-center w-7 h-7 rounded border opacity-40 cursor-not-allowed"
            style={{ borderColor: 'var(--hairline)', color: 'var(--muted)' }}
            aria-disabled="true"
          >
            <ChevronRight className="w-3.5 h-3.5" />
          </span>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/app/\(dashboard\)/processos/paginacao-processos.tsx
git commit -m "feat(ux): componente PaginacaoProcessos (Server Component, Link-based)"
```

---

## Task 13: Atualizar `ProcessosPage` com busca e paginação

**Files:**
- Modify: `src/app/(dashboard)/processos/page.tsx`

A função atual aceita `searchParams.status` e `searchParams.fase`. Adicionar `q` e `page`.

- [ ] **Step 1: Atualizar a assinatura e extrair novos searchParams**

Localizar:

```ts
export default async function ProcessosPage({
  searchParams,
}: {
  searchParams: { status?: string; fase?: string }
})
```

Substituir por:

```ts
const PAGE_SIZE = 20

export default async function ProcessosPage({
  searchParams,
}: {
  searchParams: { status?: string; fase?: string; q?: string; page?: string }
})
```

E dentro da função, adicionar após os searchParams existentes:

```ts
const q    = searchParams.q?.trim() ?? ''
const page = Math.max(1, parseInt(searchParams.page ?? '1', 10))
```

- [ ] **Step 2: Sanitizar `q` e aplicar na query**

Adicionar sanitização e full-text search. Localizar onde a query principal é construída e adicionar após os filtros de status/fase existentes:

```ts
const qSafe = q.replace(/[%_;\\]/g, '')
if (qSafe) {
  query = (query as any).or(
    `objeto.ilike.%${qSafe}%,numero_processo.ilike.%${qSafe}%`
  )
}
```

- [ ] **Step 3: Adicionar paginação e query de contagem**

Após os filtros, substituir o `.order(...)` final por:

```ts
// Query de contagem com os mesmos filtros (antes do range)
const queryCount = query
const { count: totalCount } = await (queryCount as any)
  .select('id', { count: 'exact', head: true })

const total = totalCount ?? 0

// Aplicar paginação
const { data: processos } = await (query as any)
  .order('created_at', { ascending: false })
  .range((page - 1) * PAGE_SIZE, page * PAGE_SIZE - 1)
```

**Atenção:** Supabase não suporta reutilizar a mesma query builder. Construa as duas queries separadamente com os mesmos filtros. A abordagem mais simples é duplicar a lógica de filtros numa função auxiliar dentro do arquivo, ou aplicar os filtros numa função que retorna uma query Supabase:

```ts
function aplicarFiltros(
  baseQuery: any,
  papel: string | null,
  userId: string,
  orgId: string,
  filtroStatus?: string,
  filtroFase?: string,
  qSafe?: string
) {
  let q = baseQuery

  if (papel === 'requisitante') {
    q = q.eq('criado_por', userId)
  } else {
    q = q.eq('organizacao_id', orgId)
  }

  if (filtroStatus === 'em_andamento') {
    q = q.in('status', ['rascunho', 'em_revisao'])
  } else if (filtroStatus === 'concluido') {
    q = q.in('status', ['publicado', 'assinado'])
  } else if (filtroStatus) {
    q = q.eq('status', filtroStatus)
  }

  if (filtroFase) {
    q = q.eq('fase_atual', filtroFase)
  }

  if (qSafe) {
    q = q.or(`objeto.ilike.%${qSafe}%,numero_processo.ilike.%${qSafe}%`)
  }

  return q
}
```

Usar `aplicarFiltros` tanto para a query de contagem quanto para a query de dados:

```ts
const qSafe = q.replace(/[%_;\\]/g, '')

const [{ count: totalCount }, { data: processos }] = await Promise.all([
  aplicarFiltros(
    supabase.from('processos_licitatorios').select('id', { count: 'exact', head: true }),
    papel, user.id, organizacaoId, filtroStatus, filtroFase, qSafe
  ),
  aplicarFiltros(
    supabase.from('processos_licitatorios').select('id, objeto, modalidade, status, numero_processo, valor_estimado, created_at'),
    papel, user.id, organizacaoId, filtroStatus, filtroFase, qSafe
  ).order('created_at', { ascending: false })
   .range((page - 1) * PAGE_SIZE, page * PAGE_SIZE - 1),
])

const total = totalCount ?? 0
```

- [ ] **Step 4: Adicionar BuscaProcessos e PaginacaoProcessos no JSX**

Adicionar imports no topo:

```ts
import BuscaProcessos from './busca-processos'
import PaginacaoProcessos from './paginacao-processos'
```

No JSX, localizar a linha com os filtros de status/fase existentes (botões de filtro). Acima deles, adicionar:

```tsx
<div className="flex items-center gap-3 mb-4 flex-wrap">
  <BuscaProcessos />
  {/* filtros existentes permanecem aqui */}
</div>
```

Abaixo da lista de processos (após o `map`), adicionar:

```tsx
<PaginacaoProcessos
  total={total}
  page={page}
  pageSize={PAGE_SIZE}
  q={q || undefined}
  status={filtroStatus}
  fase={filtroFase}
/>
```

- [ ] **Step 5: Verificar tipos e build**

```bash
npx tsc --noEmit
npx next build 2>&1 | tail -5
```

Esperado: build passa sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(dashboard\)/processos/page.tsx
git commit -m "feat(ux): busca full-text e paginacao na lista de processos"
```

---

## Task 14: Componente `EmptyState`

**Files:**
- Create: `src/components/licita/empty-state.tsx`

- [ ] **Step 1: Criar o arquivo**

```tsx
// src/components/licita/empty-state.tsx
import type { LucideIcon } from 'lucide-react'
import Link from 'next/link'
import { Button } from '@/components/ui/button'

interface EmptyStateCTA {
  label:    string
  href?:    string
  onClick?: () => void
}

interface EmptyStateProps {
  icon:      LucideIcon
  titulo:    string
  descricao: string
  cta?:      EmptyStateCTA
  className?: string
}

export function EmptyState({ icon: Icon, titulo, descricao, cta, className = '' }: EmptyStateProps) {
  return (
    <div
      className={`flex flex-col items-center justify-center py-14 px-6 text-center ${className}`}
    >
      <div
        className="w-12 h-12 rounded-full flex items-center justify-center mb-4"
        style={{ background: 'var(--surface)', border: '1px solid var(--hairline)' }}
      >
        <Icon className="w-6 h-6" style={{ color: 'var(--muted)' }} />
      </div>
      <p className="text-sm font-semibold mb-1" style={{ color: 'var(--ink)' }}>
        {titulo}
      </p>
      <p className="text-xs mb-5 max-w-xs" style={{ color: 'var(--muted)' }}>
        {descricao}
      </p>
      {cta && (
        cta.href ? (
          <Link href={cta.href}>
            <Button size="sm" className="h-8 text-xs">
              {cta.label}
            </Button>
          </Link>
        ) : (
          <Button size="sm" className="h-8 text-xs" onClick={cta.onClick}>
            {cta.label}
          </Button>
        )
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verificar tipos e commit**

```bash
npx tsc --noEmit
git add src/components/licita/empty-state.tsx
git commit -m "feat(ux): componente EmptyState reutilizavel"
```

---

## Task 15: Aplicar EmptyState nas telas

**Files:**
- Modify: `src/app/(dashboard)/processos/page.tsx`
- Modify: `src/app/(dashboard)/processos/[id]/cotacao/cotacao-form.tsx`
- Modify: `src/app/(dashboard)/procuradoria/lista-pareceres.tsx`
- Modify: `src/app/(dashboard)/admin/organizacoes/page.tsx`

- [ ] **Step 1: ProcessosPage — lista vazia sem filtro**

Em `processos/page.tsx`, localizar onde a lista vazia é renderizada (atualmente mostra texto simples ou nada). Substituir por:

```tsx
import { EmptyState } from '@/components/licita/empty-state'
import { FileText } from 'lucide-react'

// Dentro do JSX, após o map de processos:
{(!processos || processos.length === 0) && !qSafe && !filtroStatus && !filtroFase && (
  <EmptyState
    icon={FileText}
    titulo="Nenhum processo encontrado"
    descricao="Crie o primeiro processo licitatorio da sua organizacao."
    cta={{ label: 'Novo Processo', href: '/processos/novo' }}
  />
)}
{(!processos || processos.length === 0) && (qSafe || filtroStatus || filtroFase) && (
  <EmptyState
    icon={FileText}
    titulo="Nenhum resultado para esse filtro"
    descricao="Tente remover os filtros ou alterar o texto da busca."
    cta={{ label: 'Limpar filtros', href: '/processos' }}
  />
)}
```

- [ ] **Step 2: CotacaoForm — sem fornecedores**

Em `cotacao-form.tsx`, localizar a renderização quando `listaFornecedores.length === 0` (linha 177). Substituir o `<div>` atual por:

```tsx
import { EmptyState } from '@/components/licita/empty-state'
import { Calculator } from 'lucide-react'

// Substituir o div existente de "Nenhuma proposta adicionada":
{listaFornecedores.length === 0 && (
  <EmptyState
    icon={Calculator}
    titulo="Nenhuma proposta adicionada"
    descricao="Adicione os fornecedores consultados para calcular o valor estimado."
  />
)}
```

- [ ] **Step 3: ListaPareceres — sem pareceres pendentes**

Em `procuradoria/lista-pareceres.tsx`, localizar onde a lista vazia é tratada. Adicionar:

```tsx
import { EmptyState } from '@/components/licita/empty-state'
import { Gavel } from 'lucide-react'

// Dentro do componente, onde a lista de pareceres é renderizada:
{pareceres.length === 0 && (
  <EmptyState
    icon={Gavel}
    titulo="Nenhum parecer pendente"
    descricao="Os processos enviados para a Procuradoria apareceram aqui."
  />
)}
```

- [ ] **Step 4: AdminOrganizacoes — sem organizacoes**

Em `admin/organizacoes/page.tsx`, localizar onde a lista de orgs é renderizada. Adicionar:

```tsx
import { EmptyState } from '@/components/licita/empty-state'
import { Building2 } from 'lucide-react'

// Dentro do JSX, se orgs estiver vazio:
{(!orgs || orgs.length === 0) && (
  <EmptyState
    icon={Building2}
    titulo="Nenhuma prefeitura cadastrada"
    descricao="As prefeituras que se cadastrarem aparecao aqui aguardando aprovacao."
  />
)}
```

- [ ] **Step 5: Verificar tipos e build**

```bash
npx tsc --noEmit
npx next build 2>&1 | tail -5
```

Esperado: build passa sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/app/\(dashboard\)/processos/page.tsx \
        src/app/\(dashboard\)/processos/\[id\]/cotacao/cotacao-form.tsx \
        src/app/\(dashboard\)/procuradoria/lista-pareceres.tsx \
        src/app/\(dashboard\)/admin/organizacoes/page.tsx
git commit -m "feat(ux): empty states nas listas de processos, cotacao, procuradoria e admin"
```

---

## Task 16: Verificação Final e Push

- [ ] **Step 1: TypeScript estrito**

```bash
npx tsc --noEmit
```

Esperado: saída vazia (zero erros).

- [ ] **Step 2: Build de producao**

```bash
npx next build 2>&1 | tail -10
```

Esperado: build completo sem erros.

- [ ] **Step 3: Push**

```bash
git push origin master
```

- [ ] **Step 4: Checklist de conformidade**

- [ ] `useAutoSave` maneja `idle/dirty/saving/saved/error` e beforeunload
- [ ] `AutoSaveIndicator` exibe cada estado com texto correto, sem travessao
- [ ] EditorDFD, Riscos, ETP, TR, Edital, Parecer — todos com auto-save
- [ ] Nav do processo usa status do banco (nao_iniciado/rascunho/assinado/devolvido)
- [ ] Tooltip do nav mostra data da ultima edicao
- [ ] BuscaProcessos: debounce 300ms, limpar, URL como estado
- [ ] PaginacaoProcessos: Link-based, mostra X de Y
- [ ] EmptyState aplicado em 4 telas
- [ ] Nenhum `any` novo injustificado
- [ ] Sem travessao em textos de UI
