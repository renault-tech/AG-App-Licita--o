# Audit Log — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar sistema de auditoria de acoes dos usuarios visivel apenas para administradores em /configuracoes/logs, com filtros e exportacao CSV.

**Architecture:** Tabela `audit_log` com RLS restrita a admins. Helper `registrarAuditoria()` em `src/lib/audit/log.ts` usa service client (fire-and-forget) e e chamado nas Server Actions existentes apos operacoes bem-sucedidas. Pagina Server Component com filtros Client Component e exportacao CSV via Server Action.

**Tech Stack:** Next.js 14 App Router, Supabase (Postgres + RLS + service role), TypeScript, Server Actions, Server Components, Tailwind CSS, shadcn/ui.

---

## Mapa de Arquivos

| Arquivo | Operacao | Responsabilidade |
|---------|----------|-----------------|
| `supabase/migrations/20260526000004_audit_log.sql` | Criar | Tabela, indices e RLS |
| `src/lib/audit/log.ts` | Criar | Helper `registrarAuditoria()` |
| `src/lib/audit/labels.ts` | Criar | Mapa de codigos para labels legíveis |
| `src/lib/actions/audit.ts` | Criar | Server Action `exportarAuditoriaCsv()` |
| `src/app/(dashboard)/configuracoes/logs/page.tsx` | Criar | Pagina Server Component |
| `src/app/(dashboard)/configuracoes/logs/filtros-log.tsx` | Criar | Filtros Client Component |
| `src/app/(dashboard)/configuracoes/logs/exportar-csv.tsx` | Criar | Botao exportacao Client Component |
| `src/app/(dashboard)/configuracoes/sidebar-configuracoes.tsx` | Modificar | Adicionar item "Logs" |
| `src/lib/actions/processo.ts` | Modificar | Audit em criar e excluir processo |
| `src/lib/actions/organizacao.ts` | Modificar | Audit em atualizar org, convidar/alterar/suspender usuario |
| `src/lib/actions/dfd.ts` | Modificar | Audit em `atualizarDFD` |
| `src/lib/actions/etp.ts` | Modificar | Audit em `atualizarETP` |
| `src/lib/actions/tr.ts` | Modificar | Audit em `atualizarTR` |
| `src/lib/actions/edital.ts` | Modificar | Audit em `atualizarEdital` |
| `src/lib/actions/parecer.ts` | Modificar | Audit em `salvarParecer` |
| `src/lib/actions/tramitacao.ts` | Modificar | Audit em `enviarParaRevisao` e `aprovarDocumento` |
| `src/app/(dashboard)/processos/[id]/layout.tsx` | Modificar | Audit de acesso ao processo |

---

## Task 1: Migration — tabela audit_log

**Files:**
- Create: `supabase/migrations/20260526000004_audit_log.sql`

- [ ] **Step 1: Criar o arquivo de migration**

```sql
-- supabase/migrations/20260526000004_audit_log.sql
create table audit_log (
  id             uuid primary key default gen_random_uuid(),
  created_at     timestamptz not null default now(),
  organizacao_id uuid not null references organizacoes(id) on delete cascade,
  usuario_id     uuid references usuarios(id) on delete set null,
  nome_usuario   text not null,
  papel_usuario  text not null,
  categoria      text not null check (categoria in ('processo', 'documento', 'usuario', 'organizacao')),
  acao           text not null,
  recurso_id     uuid,
  recurso_desc   text,
  detalhes       jsonb
);

create index idx_audit_log_org_created on audit_log (organizacao_id, created_at desc);
create index idx_audit_log_usuario     on audit_log (organizacao_id, usuario_id);
create index idx_audit_log_categoria   on audit_log (organizacao_id, categoria);

alter table audit_log enable row level security;

-- Somente admins da propria organizacao podem ler
create policy "audit_log leitura admin"
  on audit_log for select
  using (
    organizacao_id = get_organizacao_id()
    and get_papel_usuario() in ('admin_organizacao', 'admin_plataforma')
  );

-- Sem policy de INSERT para usuarios — escrita exclusiva via service role no helper
```

- [ ] **Step 2: Aplicar a migration no Supabase**

Execute via MCP (ferramenta `apply_migration`) ou pelo dashboard do Supabase em SQL Editor.

Verificacao: no Supabase Table Editor, a tabela `audit_log` deve aparecer com as colunas listadas e RLS habilitado.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260526000004_audit_log.sql
git commit -m "feat(audit): migration tabela audit_log com RLS restrita a admins"
```

---

## Task 2: Helper registrarAuditoria e labels

**Files:**
- Create: `src/lib/audit/log.ts`
- Create: `src/lib/audit/labels.ts`

- [ ] **Step 1: Criar `src/lib/audit/log.ts`**

```typescript
import { createServiceClient } from '@/lib/supabase/server'

export interface AuditoriaParams {
  organizacaoId: string
  usuarioId:     string
  nomeUsuario:   string
  papelUsuario:  string
  categoria:     'processo' | 'documento' | 'usuario' | 'organizacao'
  acao:          string
  recursoId?:    string
  recursoDesc?:  string
  detalhes?:     Record<string, unknown>
}

export async function registrarAuditoria(params: AuditoriaParams): Promise<void> {
  try {
    const supabase = await createServiceClient()
    await (supabase as any).from('audit_log').insert({
      organizacao_id: params.organizacaoId,
      usuario_id:     params.usuarioId,
      nome_usuario:   params.nomeUsuario,
      papel_usuario:  params.papelUsuario,
      categoria:      params.categoria,
      acao:           params.acao,
      recurso_id:     params.recursoId   ?? null,
      recurso_desc:   params.recursoDesc ?? null,
      detalhes:       params.detalhes    ?? null,
    })
  } catch (err) {
    console.error('[audit] falha ao registrar:', err)
  }
}
```

- [ ] **Step 2: Criar `src/lib/audit/labels.ts`**

```typescript
export const LABEL_ACAO: Record<string, string> = {
  'processo.criado':        'Processo criado',
  'processo.excluido':      'Processo excluido',
  'processo.acessado':      'Processo acessado',
  'processo.tramitado':     'Processo encaminhado',
  'dfd.editado':            'DFD editado',
  'etp.editado':            'ETP editado',
  'tr.editado':             'TR editado',
  'edital.editado':         'Edital editado',
  'parecer.editado':        'Parecer editado',
  'parecer.aprovado':       'Parecer aprovado',
  'parecer.devolvido':      'Parecer devolvido',
  'usuario.convidado':      'Usuario convidado',
  'usuario.papel_alterado': 'Papel de usuario alterado',
  'usuario.suspenso':       'Usuario suspenso',
  'usuario.ativado':        'Usuario reativado',
  'organizacao.atualizada': 'Configuracoes atualizadas',
}

export const LABEL_CATEGORIA: Record<string, string> = {
  processo:     'Processo',
  documento:    'Documento',
  usuario:      'Usuario',
  organizacao:  'Organizacao',
}

export function labelAcao(acao: string): string {
  return LABEL_ACAO[acao] ?? acao
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/lib/audit/log.ts src/lib/audit/labels.ts
git commit -m "feat(audit): helper registrarAuditoria e mapa de labels"
```

---

## Task 3: Instrumentar processo.ts

**Files:**
- Modify: `src/lib/actions/processo.ts`

As funcoes `criarProcessoInicial` e `excluirProcesso` ja buscam dados do usuario. Basta adicionar o import e a chamada fire-and-forget.

- [ ] **Step 1: Adicionar import em `src/lib/actions/processo.ts`**

Adicione na linha apos os imports existentes:

```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

- [ ] **Step 2: Adicionar audit em `criarProcessoInicial`**

Localize o retorno de sucesso de `criarProcessoInicial` (apos o insert do processo) e adicione antes do `return { success: true }`:

```typescript
  // o processo foi criado com sucesso; processoId e orgId ja existem neste escopo
  void registrarAuditoria({
    organizacaoId: orgId,
    usuarioId:     user.id,
    nomeUsuario:   (userData as any).nome_completo ?? 'Usuario',
    papelUsuario:  (userData as any).papel ?? '',
    categoria:     'processo',
    acao:          'processo.criado',
    recursoId:     processoId,
    recursoDesc:   dados.objeto,
  })
```

Nota: `processoId` e `orgId` e `userData` ja existem na funcao. Se a variavel tiver nome diferente no contexto, ajuste para o nome correto.

- [ ] **Step 3: Adicionar audit em `excluirProcesso`**

Dentro da funcao `excluirProcesso`, apos a linha `revalidatePath('/processos')` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: orgId,
    usuarioId:     user.id,
    nomeUsuario:   (userData as any).nome_completo ?? 'Usuario',
    papelUsuario:  papel,
    categoria:     'processo',
    acao:          'processo.excluido',
    recursoId:     processoId,
    recursoDesc:   processo.objeto,
  })
```

- [ ] **Step 4: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/actions/processo.ts
git commit -m "feat(audit): registrar criacao e exclusao de processo"
```

---

## Task 4: Instrumentar organizacao.ts

**Files:**
- Modify: `src/lib/actions/organizacao.ts`

Todas as funcoes neste arquivo ja chamam `getUsuarioAutenticado()` que retorna o objeto completo do usuario.

- [ ] **Step 1: Adicionar import**

```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

- [ ] **Step 2: Audit em `atualizarOrganizacao`**

Apos `revalidatePath('/configuracoes/organizacao')` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo ?? 'Admin',
    papelUsuario:  usuario.papel,
    categoria:     'organizacao',
    acao:          'organizacao.atualizada',
  })
```

- [ ] **Step 3: Audit em `convidarUsuario`**

Apos `revalidatePath('/configuracoes/usuarios')` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo ?? 'Admin',
    papelUsuario:  usuario.papel,
    categoria:     'usuario',
    acao:          'usuario.convidado',
    recursoDesc:   parsed.data.email,
    detalhes:      { papel_convidado: parsed.data.papel },
  })
```

- [ ] **Step 4: Audit em `alterarPapelUsuario`**

Apos `revalidatePath('/configuracoes/usuarios')` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo ?? 'Admin',
    papelUsuario:  usuario.papel,
    categoria:     'usuario',
    acao:          'usuario.papel_alterado',
    recursoId:     parsed.data.usuario_id,
    detalhes:      { novo_papel: parsed.data.papel },
  })
```

- [ ] **Step 5: Audit em `desativarUsuario`**

Apos `revalidatePath('/configuracoes/usuarios')` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo ?? 'Admin',
    papelUsuario:  usuario.papel,
    categoria:     'usuario',
    acao:          'usuario.suspenso',
    recursoId:     usuarioId,
  })
```

- [ ] **Step 6: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add src/lib/actions/organizacao.ts
git commit -m "feat(audit): registrar acoes de organizacao e gestao de usuarios"
```

---

## Task 5: Instrumentar documentos (DFD, ETP, TR, Edital)

**Files:**
- Modify: `src/lib/actions/dfd.ts`
- Modify: `src/lib/actions/etp.ts`
- Modify: `src/lib/actions/tr.ts`
- Modify: `src/lib/actions/edital.ts`

`atualizarDFD` ja chama `obterUsuarioEOrg()`. As demais precisam ter contexto de usuario adicionado.

- [ ] **Step 1: Audit em `atualizarDFD` (dfd.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

Em `atualizarDFD`, apos `revalidatePath('/dashboard')` e antes do `return { success: true }`:

```typescript
  const { usuario } = ctx
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo,
    papelUsuario:  usuario.papel,
    categoria:     'documento',
    acao:          'dfd.editado',
    recursoId:     dfdId,
  })
```

- [ ] **Step 2: Audit em `atualizarETP` (etp.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

Substitua a funcao `atualizarETP` por:

```typescript
export async function atualizarETP(etpId: string, dados: Partial<ETPRow>) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false as const, error: 'Nao autenticado.' }

  const { data: usuarioData } = await supabase
    .from('usuarios')
    .select('id, nome_completo, papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  const { error } = await (supabase as any)
    .from('etp')
    .update({ ...dados, updated_at: new Date().toISOString() })
    .eq('id', etpId)

  if (error) return { success: false as const, error: error.message as string }

  if (usuarioData) {
    const u = usuarioData as any
    void registrarAuditoria({
      organizacaoId: u.organizacao_id,
      usuarioId:     user.id,
      nomeUsuario:   u.nome_completo,
      papelUsuario:  u.papel,
      categoria:     'documento',
      acao:          'etp.editado',
      recursoId:     etpId,
    })
  }

  revalidatePath('/dashboard')
  return { success: true as const }
}
```

- [ ] **Step 3: Audit em `atualizarTR` (tr.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

Substitua a funcao `atualizarTR` pelo mesmo padrao de `atualizarETP`, trocando `'etp'` por `'termo_referencia'` e `'etp.editado'` por `'tr.editado'`:

```typescript
export async function atualizarTR(trId: string, dados: Partial<TermoReferenciaRow>) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false as const, error: 'Nao autenticado.' }

  const { data: usuarioData } = await supabase
    .from('usuarios')
    .select('id, nome_completo, papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  const { error } = await (supabase as any)
    .from('termo_referencia')
    .update({ ...dados, updated_at: new Date().toISOString() })
    .eq('id', trId)

  if (error) return { success: false as const, error: error.message as string }

  if (usuarioData) {
    const u = usuarioData as any
    void registrarAuditoria({
      organizacaoId: u.organizacao_id,
      usuarioId:     user.id,
      nomeUsuario:   u.nome_completo,
      papelUsuario:  u.papel,
      categoria:     'documento',
      acao:          'tr.editado',
      recursoId:     trId,
    })
  }

  revalidatePath('/dashboard')
  return { success: true as const }
}
```

- [ ] **Step 4: Audit em `atualizarEdital` (edital.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

Aplique o mesmo padrao a `atualizarEdital`, com `acao: 'edital.editado'` e a tabela `edital`. A funcao `atualizarEdital` recebe `editalId` e `conteudo` — adicione o fetch de usuario e a chamada apos o update bem-sucedido.

- [ ] **Step 5: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/lib/actions/dfd.ts src/lib/actions/etp.ts src/lib/actions/tr.ts src/lib/actions/edital.ts
git commit -m "feat(audit): registrar edicao de DFD, ETP, TR e Edital"
```

---

## Task 6: Instrumentar parecer.ts e tramitacao.ts

**Files:**
- Modify: `src/lib/actions/parecer.ts`
- Modify: `src/lib/actions/tramitacao.ts`

- [ ] **Step 1: Audit em `salvarParecer` (parecer.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

A funcao `salvarParecer` nao busca o usuario. Adicione o fetch e a chamada:

```typescript
export async function salvarParecer(parecerId: string, conteudo: string, status: StatusParecer) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Nao autenticado.' }

  const { data: usuarioData } = await supabase
    .from('usuarios')
    .select('id, nome_completo, papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  const supabaseAny = supabase as any

  const { error } = await supabaseAny
    .from('pareceres')
    .update({ conteudo, status, updated_at: new Date().toISOString() })
    .eq('id', parecerId)

  if (error) return { success: false, error: error.message as string }

  if (usuarioData) {
    const u = usuarioData as any
    const acaoFinal = status === 'aprovado'
      ? 'parecer.aprovado'
      : status === 'devolvido'
      ? 'parecer.devolvido'
      : 'parecer.editado'
    void registrarAuditoria({
      organizacaoId: u.organizacao_id,
      usuarioId:     user.id,
      nomeUsuario:   u.nome_completo,
      papelUsuario:  u.papel,
      categoria:     'documento',
      acao:          acaoFinal,
      recursoId:     parecerId,
    })
  }

  revalidatePath('/dashboard')
  return { success: true }
}
```

- [ ] **Step 2: Audit em `enviarParaRevisao` (tramitacao.ts)**

Adicione o import:
```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

Em `enviarParaRevisao`, apos `revalidatePath(...)` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo,
    papelUsuario:  usuario.papel,
    categoria:     'processo',
    acao:          'processo.tramitado',
    recursoId:     processoId,
    detalhes:      { tabela, documentoId, tipo: 'envio_revisao' },
  })
```

- [ ] **Step 3: Audit em `aprovarDocumento` (tramitacao.ts)**

Apos `revalidatePath(...)` e antes do `return { success: true }`:

```typescript
  void registrarAuditoria({
    organizacaoId: usuario.organizacao_id,
    usuarioId:     usuario.id,
    nomeUsuario:   usuario.nome_completo,
    papelUsuario:  usuario.papel,
    categoria:     'processo',
    acao:          'processo.tramitado',
    recursoId:     processoId,
    detalhes:      { tabela, documentoId, tipo: 'aprovacao' },
  })
```

- [ ] **Step 4: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/actions/parecer.ts src/lib/actions/tramitacao.ts
git commit -m "feat(audit): registrar acoes de parecer e tramitacao de processo"
```

---

## Task 7: Registrar acesso ao processo no layout

**Files:**
- Modify: `src/app/(dashboard)/processos/[id]/layout.tsx`

O layout ja e um Server Component que autentica o usuario. Adicionar o registro de acesso e um fetch adicional do nome do usuario.

- [ ] **Step 1: Adicionar import**

Em `src/app/(dashboard)/processos/[id]/layout.tsx`, adicione:

```typescript
import { registrarAuditoria } from '@/lib/audit/log'
```

- [ ] **Step 2: Adicionar fetch de nome do usuario e chamada de audit**

O layout ja chama `obterPapelUsuario()` e tem acesso ao supabase. Adicione, apos a obtencao do processo (onde `processo.objeto` estiver disponivel) e antes do return do JSX:

```typescript
  // Registrar acesso — fire-and-forget, nao bloqueia renderizacao
  const { data: usuarioAudit } = await supabase
    .from('usuarios')
    .select('nome_completo, papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  if (usuarioAudit) {
    const ua = usuarioAudit as any
    void registrarAuditoria({
      organizacaoId: ua.organizacao_id,
      usuarioId:     user.id,
      nomeUsuario:   ua.nome_completo ?? 'Usuario',
      papelUsuario:  ua.papel ?? '',
      categoria:     'processo',
      acao:          'processo.acessado',
      recursoId:     params.id,
      recursoDesc:   (processo as any).objeto ?? '',
    })
  }
```

Nota: `user` ja esta disponivel no layout via `supabase.auth.getUser()`. `params.id` e o ID do processo. Ajuste os nomes de variavel conforme o contexto real do arquivo.

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/processos/[id]/layout.tsx"
git commit -m "feat(audit): registrar acesso ao processo no layout"
```

---

## Task 8: Server Action exportarAuditoriaCsv

**Files:**
- Create: `src/lib/actions/audit.ts`

- [ ] **Step 1: Criar `src/lib/actions/audit.ts`**

```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import { labelAcao } from '@/lib/audit/labels'

export interface FiltrosAudit {
  de?:          string  // ISO date string, ex: '2026-01-01'
  ate?:         string  // ISO date string
  usuarioId?:   string
  categoria?:   string
}

export async function exportarAuditoriaCsv(
  filtros: FiltrosAudit
): Promise<{ success: boolean; csv?: string; error?: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { success: false, error: 'Nao autenticado.' }

  const { data: usuarioData } = await supabase
    .from('usuarios')
    .select('papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  const u = usuarioData as any
  if (!u || !['admin_organizacao', 'admin_plataforma'].includes(u.papel)) {
    return { success: false, error: 'Sem permissao.' }
  }

  const de  = filtros.de  ?? new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString()
  const ate = filtros.ate ?? new Date().toISOString()

  let query = (supabase as any)
    .from('audit_log')
    .select('created_at, nome_usuario, papel_usuario, categoria, acao, recurso_desc, detalhes')
    .eq('organizacao_id', u.organizacao_id)
    .gte('created_at', de)
    .lte('created_at', ate + 'T23:59:59Z')
    .order('created_at', { ascending: false })
    .limit(10000)

  if (filtros.usuarioId) query = query.eq('usuario_id', filtros.usuarioId)
  if (filtros.categoria) query = query.eq('categoria', filtros.categoria)

  const { data: rows, error } = await query
  if (error) return { success: false, error: error.message }

  const linhas = (rows as any[]).map(r => {
    const dt = new Date(r.created_at)
    const data   = dt.toLocaleDateString('pt-BR')
    const hora   = dt.toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' })
    const det    = r.detalhes ? JSON.stringify(r.detalhes) : ''
    return [
      data,
      hora,
      escapeCsv(r.nome_usuario),
      escapeCsv(r.papel_usuario),
      escapeCsv(r.categoria),
      escapeCsv(labelAcao(r.acao)),
      escapeCsv(r.recurso_desc ?? ''),
      escapeCsv(det),
    ].join(',')
  })

  const cabecalho = 'Data,Hora,Usuario,Papel,Categoria,Acao,Recurso,Detalhes'
  const csv = [cabecalho, ...linhas].join('\n')

  return { success: true, csv }
}

function escapeCsv(value: string): string {
  if (value.includes(',') || value.includes('"') || value.includes('\n')) {
    return `"${value.replace(/"/g, '""')}"`
  }
  return value
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/actions/audit.ts
git commit -m "feat(audit): server action exportarAuditoriaCsv"
```

---

## Task 9: Pagina /configuracoes/logs

**Files:**
- Create: `src/app/(dashboard)/configuracoes/logs/page.tsx`

- [ ] **Step 1: Criar `page.tsx`**

```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { ScrollText } from 'lucide-react'
import { labelAcao, LABEL_CATEGORIA } from '@/lib/audit/labels'
import FiltrosLog from './filtros-log'
import ExportarCsv from './exportar-csv'
import type { FiltrosAudit } from '@/lib/actions/audit'

const PAGE_SIZE = 20

interface Props {
  searchParams: {
    de?:        string
    ate?:       string
    usuario_id?: string
    categoria?: string
    page?:      string
  }
}

export default async function LogsPage({ searchParams }: Props) {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: usuarioData } = await supabase
    .from('usuarios')
    .select('papel, organizacao_id')
    .eq('id', user.id)
    .maybeSingle()

  const u = usuarioData as any
  if (!u || !['admin_organizacao', 'admin_plataforma'].includes(u.papel)) {
    redirect('/configuracoes/ia')
  }

  const hoje = new Date().toISOString().slice(0, 10)
  const haMes = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString().slice(0, 10)

  const de       = searchParams.de        ?? haMes
  const ate      = searchParams.ate       ?? hoje
  const usuId    = searchParams.usuario_id
  const catFiltro= searchParams.categoria
  const page     = Math.max(1, parseInt(searchParams.page ?? '1', 10))
  const offset   = (page - 1) * PAGE_SIZE

  let query = (supabase as any)
    .from('audit_log')
    .select('id, created_at, nome_usuario, papel_usuario, categoria, acao, recurso_desc, detalhes', { count: 'exact' })
    .eq('organizacao_id', u.organizacao_id)
    .gte('created_at', de)
    .lte('created_at', ate + 'T23:59:59Z')
    .order('created_at', { ascending: false })
    .range(offset, offset + PAGE_SIZE - 1)

  if (usuId)      query = query.eq('usuario_id', usuId)
  if (catFiltro)  query = query.eq('categoria', catFiltro)

  const { data: logs, count } = await query
  const total   = count ?? 0
  const paginas = Math.ceil(total / PAGE_SIZE)
  const lista   = (logs as any[]) ?? []

  // Buscar lista de usuarios para o filtro
  const { data: usuariosOrg } = await supabase
    .from('usuarios')
    .select('id, nome_completo')
    .eq('organizacao_id', u.organizacao_id)
    .eq('ativo', true)
    .order('nome_completo')

  const filtrosAtivos: FiltrosAudit = {
    de, ate, usuarioId: usuId, categoria: catFiltro,
  }

  return (
    <div className="space-y-6">
      {/* Cabecalho */}
      <div className="flex items-start justify-between gap-4 flex-wrap">
        <div>
          <div className="flex items-center gap-2 mb-1">
            <ScrollText className="w-4 h-4" style={{ color: 'var(--accent)' }} />
            <span
              className="text-[10px] font-bold tracking-[0.12em] uppercase"
              style={{ color: 'var(--accent)' }}
            >
              Seguranca
            </span>
          </div>
          <h1
            className="text-xl font-bold"
            style={{ color: 'var(--ink)', fontFamily: 'var(--font-heading)' }}
          >
            Log de Auditoria
          </h1>
          <p className="text-sm mt-1" style={{ color: 'var(--muted)' }}>
            {total} registro{total !== 1 ? 's' : ''} encontrado{total !== 1 ? 's' : ''}
          </p>
        </div>
        <ExportarCsv filtros={filtrosAtivos} />
      </div>

      {/* Filtros */}
      <FiltrosLog
        usuarios={(usuariosOrg as any[]) ?? []}
        de={de}
        ate={ate}
        usuarioId={usuId}
        categoria={catFiltro}
      />

      {/* Tabela */}
      <div
        className="rounded-[var(--r-lg)] border overflow-hidden"
        style={{ background: 'var(--surface)', borderColor: 'var(--hairline)' }}
      >
        {lista.length === 0 ? (
          <div className="px-6 py-12 text-center">
            <p className="text-sm" style={{ color: 'var(--muted)' }}>
              Nenhum registro encontrado para os filtros selecionados.
            </p>
          </div>
        ) : (
          <div className="overflow-x-auto">
            <table className="w-full text-sm">
              <thead>
                <tr style={{ background: 'var(--surfaceAlt)', borderBottom: '1px solid var(--hairline)' }}>
                  {['Data/Hora', 'Usuario', 'Acao', 'Recurso'].map(h => (
                    <th
                      key={h}
                      className="px-4 py-3 text-left text-[11px] font-bold tracking-wider uppercase"
                      style={{ color: 'var(--muted)' }}
                    >
                      {h}
                    </th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {lista.map((row: any) => {
                  const dt = new Date(row.created_at)
                  const data = dt.toLocaleDateString('pt-BR')
                  const hora = dt.toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' })
                  return (
                    <tr
                      key={row.id}
                      style={{ borderBottom: '1px solid var(--hairlineSoft)' }}
                    >
                      <td className="px-4 py-3 whitespace-nowrap">
                        <span className="font-medium" style={{ color: 'var(--ink)' }}>{data}</span>
                        <span className="ml-1.5 text-[11px]" style={{ color: 'var(--muted)' }}>{hora}</span>
                      </td>
                      <td className="px-4 py-3">
                        <p className="font-medium truncate max-w-[160px]" style={{ color: 'var(--ink)' }}>
                          {row.nome_usuario}
                        </p>
                        <p className="text-[11px]" style={{ color: 'var(--muted)' }}>
                          {row.papel_usuario}
                        </p>
                      </td>
                      <td className="px-4 py-3">
                        <span
                          className="inline-flex items-center px-2 py-0.5 rounded-full text-[11px] font-semibold"
                          style={{ background: 'var(--primaryWash)', color: 'var(--primary)' }}
                        >
                          {labelAcao(row.acao)}
                        </span>
                        <p className="text-[11px] mt-0.5" style={{ color: 'var(--muted)' }}>
                          {LABEL_CATEGORIA[row.categoria] ?? row.categoria}
                        </p>
                      </td>
                      <td className="px-4 py-3 max-w-[200px]">
                        <p className="text-[13px] truncate" style={{ color: 'var(--inkSoft)' }}>
                          {row.recurso_desc ?? '—'}
                        </p>
                      </td>
                    </tr>
                  )
                })}
              </tbody>
            </table>
          </div>
        )}

        {/* Paginacao */}
        {paginas > 1 && (
          <div
            className="flex items-center justify-between px-4 py-3"
            style={{ borderTop: '1px solid var(--hairline)', background: 'var(--surfaceAlt)' }}
          >
            <p className="text-xs" style={{ color: 'var(--muted)' }}>
              Pagina {page} de {paginas} — {total} registros
            </p>
            <div className="flex gap-2">
              {page > 1 && (
                <a
                  href={`?de=${de}&ate=${ate}${usuId ? `&usuario_id=${usuId}` : ''}${catFiltro ? `&categoria=${catFiltro}` : ''}&page=${page - 1}`}
                  className="text-xs px-3 py-1.5 rounded-[var(--r-md)] border transition-colors"
                  style={{ borderColor: 'var(--hairline)', color: 'var(--inkSoft)' }}
                >
                  Anterior
                </a>
              )}
              {page < paginas && (
                <a
                  href={`?de=${de}&ate=${ate}${usuId ? `&usuario_id=${usuId}` : ''}${catFiltro ? `&categoria=${catFiltro}` : ''}&page=${page + 1}`}
                  className="text-xs px-3 py-1.5 rounded-[var(--r-md)] border transition-colors"
                  style={{ borderColor: 'var(--hairline)', color: 'var(--inkSoft)' }}
                >
                  Proxima
                </a>
              )}
            </div>
          </div>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/logs/page.tsx"
git commit -m "feat(audit): pagina /configuracoes/logs com lista paginada"
```

---

## Task 10: Filtros (Client Component)

**Files:**
- Create: `src/app/(dashboard)/configuracoes/logs/filtros-log.tsx`

- [ ] **Step 1: Criar `filtros-log.tsx`**

```typescript
'use client'

import { useRouter, usePathname } from 'next/navigation'

interface Props {
  usuarios:   { id: string; nome_completo: string }[]
  de:         string
  ate:        string
  usuarioId?: string
  categoria?: string
}

const CATEGORIAS = [
  { value: '',            label: 'Todas as categorias' },
  { value: 'processo',    label: 'Processo'            },
  { value: 'documento',   label: 'Documento'           },
  { value: 'usuario',     label: 'Usuario'             },
  { value: 'organizacao', label: 'Organizacao'         },
]

export default function FiltrosLog({ usuarios, de, ate, usuarioId, categoria }: Props) {
  const router   = useRouter()
  const pathname = usePathname()

  function aplicar(overrides: Record<string, string>) {
    const params = new URLSearchParams()
    const vals = { de, ate, usuario_id: usuarioId ?? '', categoria: categoria ?? '', ...overrides }
    Object.entries(vals).forEach(([k, v]) => { if (v) params.set(k, v) })
    params.delete('page')
    router.push(`${pathname}?${params.toString()}`)
  }

  return (
    <div
      className="flex flex-wrap gap-3 p-4 rounded-[var(--r-lg)] border"
      style={{ background: 'var(--surfaceAlt)', borderColor: 'var(--hairline)' }}
    >
      <div className="flex flex-col gap-1">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: 'var(--muted)' }}>
          De
        </label>
        <input
          type="date"
          defaultValue={de}
          onChange={e => aplicar({ de: e.target.value })}
          className="text-sm px-3 py-1.5 rounded-[var(--r-md)] border"
          style={{ borderColor: 'var(--hairline)', background: 'var(--surface)', color: 'var(--ink)' }}
        />
      </div>

      <div className="flex flex-col gap-1">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: 'var(--muted)' }}>
          Ate
        </label>
        <input
          type="date"
          defaultValue={ate}
          onChange={e => aplicar({ ate: e.target.value })}
          className="text-sm px-3 py-1.5 rounded-[var(--r-md)] border"
          style={{ borderColor: 'var(--hairline)', background: 'var(--surface)', color: 'var(--ink)' }}
        />
      </div>

      <div className="flex flex-col gap-1">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: 'var(--muted)' }}>
          Usuario
        </label>
        <select
          defaultValue={usuarioId ?? ''}
          onChange={e => aplicar({ usuario_id: e.target.value })}
          className="text-sm px-3 py-1.5 rounded-[var(--r-md)] border"
          style={{ borderColor: 'var(--hairline)', background: 'var(--surface)', color: 'var(--ink)' }}
        >
          <option value="">Todos os usuarios</option>
          {usuarios.map(u => (
            <option key={u.id} value={u.id}>{(u as any).nome_completo}</option>
          ))}
        </select>
      </div>

      <div className="flex flex-col gap-1">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: 'var(--muted)' }}>
          Categoria
        </label>
        <select
          defaultValue={categoria ?? ''}
          onChange={e => aplicar({ categoria: e.target.value })}
          className="text-sm px-3 py-1.5 rounded-[var(--r-md)] border"
          style={{ borderColor: 'var(--hairline)', background: 'var(--surface)', color: 'var(--ink)' }}
        >
          {CATEGORIAS.map(c => (
            <option key={c.value} value={c.value}>{c.label}</option>
          ))}
        </select>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/logs/filtros-log.tsx"
git commit -m "feat(audit): componente de filtros do log de auditoria"
```

---

## Task 11: Botao Exportar CSV (Client Component)

**Files:**
- Create: `src/app/(dashboard)/configuracoes/logs/exportar-csv.tsx`

- [ ] **Step 1: Criar `exportar-csv.tsx`**

```typescript
'use client'

import { useState } from 'react'
import { Download, Loader2 } from 'lucide-react'
import { toast } from 'sonner'
import { exportarAuditoriaCsv } from '@/lib/actions/audit'
import type { FiltrosAudit } from '@/lib/actions/audit'

export default function ExportarCsv({ filtros }: { filtros: FiltrosAudit }) {
  const [exportando, setExportando] = useState(false)

  async function handleExportar() {
    setExportando(true)
    const result = await exportarAuditoriaCsv(filtros)
    if (!result.success || !result.csv) {
      toast.error(result.error ?? 'Erro ao exportar.')
      setExportando(false)
      return
    }

    const blob = new Blob([result.csv], { type: 'text/csv;charset=utf-8;' })
    const url  = URL.createObjectURL(blob)
    const a    = document.createElement('a')
    const hoje = new Date().toISOString().slice(0, 10)
    a.href     = url
    a.download = `audit-log-${hoje}.csv`
    a.click()
    URL.revokeObjectURL(url)
    setExportando(false)
    toast.success('CSV exportado com sucesso.')
  }

  return (
    <button
      type="button"
      onClick={handleExportar}
      disabled={exportando}
      className="inline-flex items-center gap-2 px-4 py-2 text-sm font-semibold rounded-[var(--r-md)] border transition-colors disabled:opacity-60"
      style={{ borderColor: 'var(--hairline)', color: 'var(--inkSoft)', background: 'var(--surface)' }}
    >
      {exportando
        ? <Loader2 className="w-4 h-4 animate-spin" />
        : <Download className="w-4 h-4" />
      }
      {exportando ? 'Exportando...' : 'Exportar CSV'}
    </button>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/logs/exportar-csv.tsx"
git commit -m "feat(audit): botao de exportacao CSV do log de auditoria"
```

---

## Task 12: Sidebar + verificacao final

**Files:**
- Modify: `src/app/(dashboard)/configuracoes/sidebar-configuracoes.tsx`

- [ ] **Step 1: Adicionar item "Logs" ao NAV_ADMIN**

Em `sidebar-configuracoes.tsx`, adicione o import do icone e o item no array `NAV_ADMIN`:

```typescript
import { Building2, Users, Bot, Settings2, PenTool, Lock, Radio, ScrollText } from 'lucide-react'
```

No array `NAV_ADMIN`, adicione o item apos `assinatura-eletronica`:

```typescript
  { href: '/configuracoes/logs', label: 'Logs de Auditoria', icon: ScrollText },
```

O array completo fica:

```typescript
const NAV_ADMIN = [
  { href: '/configuracoes/organizacao',          label: 'Organizacao',            icon: Settings2    },
  { href: '/configuracoes/secretarias',           label: 'Secretarias',            icon: Building2    },
  { href: '/configuracoes/usuarios',              label: 'Usuarios',               icon: Users        },
  { href: '/configuracoes/permissoes',            label: 'Permissoes',             icon: Lock         },
  { href: '/configuracoes/ia',                    label: 'Inteligencia Artificial', icon: Bot         },
  { href: '/configuracoes/faixa-de-informacoes', label: 'Faixa de Informacoes',   icon: Radio        },
  { href: '/configuracoes/assinatura-eletronica', label: 'Assinatura',             icon: PenTool      },
  { href: '/configuracoes/logs',                  label: 'Logs de Auditoria',      icon: ScrollText   },
]
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Build de verificacao**

```bash
npx next build 2>&1 | tail -20
```

Esperado: build sem erros. Warnings de lint sao aceitaveis.

- [ ] **Step 4: Commit final**

```bash
git add "src/app/(dashboard)/configuracoes/sidebar-configuracoes.tsx"
git commit -m "feat(audit): adicionar Logs de Auditoria ao sidebar de configuracoes"
git push origin master
```

---

## Verificacao manual

Apos o deploy, verificar:

1. Login como `admin_organizacao`. Navegar ate `/configuracoes/logs`. A pagina deve carregar sem erro.
2. Executar qualquer acao auditada (ex: editar o DFD de um processo). Recarregar `/configuracoes/logs`. O registro deve aparecer.
3. Aplicar filtro por categoria "documento". Apenas registros de documento devem aparecer.
4. Clicar em "Exportar CSV". O download deve iniciar com arquivo `audit-log-YYYY-MM-DD.csv`.
5. Login como `requisitante`. Acessar `/configuracoes/logs` diretamente pela URL. Deve redirecionar para `/configuracoes/ia`.
6. Confirmar no Supabase que a tabela `audit_log` tem registros com `nome_usuario` e `papel_usuario` preenchidos corretamente.
